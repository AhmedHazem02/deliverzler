# تحديثات صفحة تفاصيل الطلب

## التغييرات المُنفذة

### 1. إضافة عرض الحاجات المطلوبة (Order Items)
تم إنشاء نظام كامل لجلب وعرض المنتجات الموجودة في كل طلب من collection `order_items`:

#### الملفات الجديدة:
- **`order_items_remote_data_source.dart`**: Data source لجلب الـ order items من Firestore
- **`order_items_repo.dart`**: Repository layer لتحويل DTOs إلى domain models
- **`order_items_provider.dart`**: Riverpod provider لإدارة حالة الـ order items

### 2. إضافة معلومات المتجر (Store Information)
تم تحديث الـ Order model لدعم معلومات المتجر:

#### التعديلات:
- إضافة حقول `storeId`, `storeName`, `storeAddress` في `AppOrder` domain model
- تحديث `OrderDto` لقراءة `store_id` من Firestore
- عرض معرف المتجر في صفحة التفاصيل

### 3. تصميم جديد لصفحة التفاصيل (OrderDetailsDialog)
تم إعادة تصميم صفحة التفاصيل بالكامل بشكل عصري وجذاب:

#### المميزات:
✅ تصميم modern مع gradients وألوان جذابة
✅ Responsive للموبايل
✅ عرض المنتجات مع الصور والكميات والأسعار
✅ عرض المبلغ الفرعي (Subtotal) ورسوم التوصيل والمبلغ الإجمالي
✅ عرض معلومات المتجر
✅ عرض تفاصيل العميل والعنوان
✅ Loading states و Error handling
✅ Icons وألوان واضحة لكل قسم

## البيانات المعروضة

### 📦 المنتجات المطلوبة
- صورة المنتج
- اسم المنتج
- الكمية
- السعر لكل وحدة
- السعر الإجمالي

### 💰 ملخص الأسعار
- المجموع الفرعي (Subtotal)
- رسوم التوصيل (Delivery Fee)
- المبلغ الإجمالي (Total)

### 🏪 معلومات المتجر
- معرف المتجر (Store ID)

### 👤 معلومات العميل
- الاسم
- العنوان (المحافظة، المدينة، الشارع)
- رقم الهاتف

### 📝 ملاحظات
- ملاحظات العميل (إن وجدت)

## كيفية استخدام المنتجات

الـ order items يتم جلبها تلقائياً من collection `order_items` في Firestore بناءً على `order_id`.

### Structure المطلوب في Firestore:

```
order_items (collection)
├── {document_id}
    ├── order_id: "AUeXv9jWdCayKvhMayPa"
    ├── product_id: "BZZsPvJDCvUxp69EAYqV"
    ├── name: "Product Name"
    ├── price: 40000
    ├── quantity: 1
    ├── total: 40000
    ├── imageUrl: "https://..."
    ├── description: "..." (optional)
    └── category: "..." (optional)
```

## ملاحظات مهمة

1. **Lazy Loading**: الـ order items يتم جلبها فقط عند فتح صفحة التفاصيل لتحسين الأداء
2. **Real-time Updates**: يمكن استخدام `orderItemsStreamProvider` للحصول على تحديثات فورية
3. **Error Handling**: النظام يعرض رسائل خطأ واضحة في حالة فشل جلب البيانات
4. **Store Info**: حالياً يتم عرض Store ID فقط، يمكن لاحقاً إضافة integration كامل لجلب اسم وعنوان المتجر

## التحسينات المستقبلية المقترحة

1. إضافة collection للمتاجر وجلب بيانات المتجر الكاملة
2. عرض صورة المتجر
3. إضافة زر للاتصال بالمتجر
4. إضافة خريطة توضح موقع التوصيل
5. إضافة timeline لحالات الطلب

## الملفات المعدلة

### ملفات جديدة:
- `lib/features/home/infrastructure/data_sources/order_items_remote_data_source.dart`
- `lib/features/home/infrastructure/repos/order_items_repo.dart`
- `lib/features/home/presentation/providers/order_items_provider.dart`

### ملفات معدلة:
- `lib/features/home/domain/order.dart` - إضافة حقول المتجر
- `lib/features/home/infrastructure/dtos/order_dto.dart` - mapping لـ store_id
- `lib/features/home/presentation/components/dialogs/order_details_dialog.dart` - إعادة تصميم كاملة

## الأوامر المطلوبة

تم تشغيل `build_runner` بنجاح لتوليد الكود المطلوب:

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

## الاختبار

للتأكد من أن كل شيء يعمل بشكل صحيح:

1. تأكد من وجود بيانات في collection `order_items` في Firestore
2. افتح التطبيق واضغط على "عرض التفاصيل" لأي طلب
3. تحقق من ظهور:
   - المنتجات بشكل صحيح
   - الأسعار
   - معلومات المتجر
   - معلومات العميل
